# Estágio 1: Build
FROM node:18-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Se estiver usando Vite, o comando gera a pasta 'dist'. Se for CRA, gera 'build'.
RUN npm run build

# Estágio 2: Servidor Web (Nginx)
FROM nginx:alpine
# Copia os arquivos estáticos gerados. 
# ATENÇÃO: Se usar Create React App, mude de /app/dist para /app/build
COPY --from=build /app/dist /usr/share/nginx/html

# Copia configuração customizada do Nginx (vamos criar esse arquivo abaixo)
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]